# To-do list

- See [GitHub issues](https://github.com/springfall2008/batpred/issues)

## Future Enhancements
- **Clipping Buffer Time Window Configuration:** Currently, the system automatically determines the start and stop times for the clipping buffer based on predicted solar clipping. We should consider adding manual configuration overrides (e.g., `clipping_buffer_start_time`, `clipping_buffer_end_time`) to allow users to explicitly set or adjust the active window.
